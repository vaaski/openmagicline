{
  "watch": ["src", "types"],
  "ext": "ts, json",
  "exec": "npx ts-node -r dotenv/config ./src/socket.ts",
  "events": {
    "start": "node -e console.clear()"
  },
  "env": {
    "DEBUG": "openmagicline*"
  }
}
